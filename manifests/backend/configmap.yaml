apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  labels:
    app: backend
    tier: backend
data:
  # Role used in logs/metrics to identify this component.
  ROLE: "backend"


