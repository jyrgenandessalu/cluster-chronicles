apiVersion: v1
kind: Pod
metadata:
  name: crash-loop-test
spec:
  containers:
    - name: crash-loop
      image: busybox
      command: ["sh", "-c"]
      args: ["sleep 10; exit 1"]  # Sleep for 10 seconds then exit with error
  restartPolicy: Always  # Always restart on failure

