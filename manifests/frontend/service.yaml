apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
    tier: frontend
spec:
  # ClusterIP for internal access; external access is via Ingress.
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 5000
      targetPort: http


