apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  labels:
    app: frontend
    tier: frontend
data:
  ROLE: "frontend"
  # Backend URL is provided here for future use by the frontend application.
  BACKEND_URL: "http://backend:5000"


